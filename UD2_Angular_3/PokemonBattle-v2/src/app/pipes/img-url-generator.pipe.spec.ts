import { ImgUrlGeneratorPipe } from './img-url-generator.pipe';

describe('ImgUrlGeneratorPipe', () => {
  it('create an instance', () => {
    const pipe = new ImgUrlGeneratorPipe();
    expect(pipe).toBeTruthy();
  });
});
